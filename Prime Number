variable:
remainder
divisor
number
initialize:
number=1
divisor=2
if number<2 and number <100
  print "its not prime"
  number+=1
  proccess again
  if number=2
     print "its prime"
     number +=1
     proccess again
  if number>2
      remainder=number%divisor
   if remainder=0 
      print "its not prime"
      number+=1
      advisor=2
       proccess again
   else
        divisor+=1
        if divisor=number
         print "its prime"
         number+=1
         advisor=2
         proccess again
      else 
         proccess again
  
